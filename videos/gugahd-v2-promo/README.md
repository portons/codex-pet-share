# Gugahd v2 X promo

A 5.5-second, 1080×1080 H.264 promo rendered from gugahd's live v2 atlas.
It uses all 16 look-direction cells and is silent for autoplay-friendly posting
on X.

Run `./render.sh` to refresh the source atlas and rebuild the clip.
